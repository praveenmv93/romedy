services:
  - type: web
    name: romedy-coffee-date
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn equipo.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
